<Window x:Class="SmilezStrap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmilezStrap" Height="450" Width="700"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Menu Button Style -->
        <Style x:Key="MenuButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2A2A"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Action Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="20,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1084D8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="20,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#353535"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="Transparent" Padding="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>

        <Border Background="#1E1E1E" CornerRadius="8" BorderThickness="1" BorderBrush="#2A2A2A">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Sidebar -->
                <Border Grid.Column="0" Background="#252525" CornerRadius="8,0,0,8">
                    <Grid>
                        <!-- Title Bar (Left Part) -->
                        <Grid Height="40" VerticalAlignment="Top" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                            <StackPanel Orientation="Horizontal" Margin="15,0" VerticalAlignment="Center">
                                <TextBlock Text="SmilezStrap" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>

                        <!-- Menu Items -->
                        <StackPanel Margin="0,50,0,0">
                            <Button Style="{StaticResource MenuButton}" Background="#2A2A2A" Foreground="White">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🏠" FontSize="14" Margin="0,0,10,0"/>
                                    <TextBlock Text="Home"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource MenuButton}" Click="Settings_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⚙" FontSize="14" Margin="0,0,10,0"/>
                                    <TextBlock Text="Settings"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource MenuButton}" Click="About_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="ℹ" FontSize="14" Margin="0,0,10,0"/>
                                    <TextBlock Text="About"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <!-- Bottom Links -->
                        <StackPanel VerticalAlignment="Bottom" Margin="15,0,15,15">
                            <Border Height="1" Background="#2A2A2A" Margin="0,0,0,10"/>
                            <Button Style="{StaticResource MenuButton}" Click="Discord_Click" Padding="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💬" FontSize="12" Margin="0,0,8,0"/>
                                    <TextBlock Text="Discord" FontSize="12"/>
                                </StackPanel>
                            </Button>
                            <TextBlock Text="v1.0.2" Foreground="#555" FontSize="10" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Right Content Area -->
                <Grid Grid.Column="1">
                    <!-- Title Bar (Right Part) -->
                    <Grid Height="40" VerticalAlignment="Top" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,0">
                            <Button Content="─" Width="40" Height="40" Background="Transparent" BorderThickness="0"
                                    Foreground="#999" FontSize="13" Click="MinimizeButton_Click" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2A2A2A"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="✕" Width="40" Height="40" Background="Transparent" BorderThickness="0"
                                    Foreground="#999" FontSize="13" Click="CloseButton_Click" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#C42B1C"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- Main Content -->
                    <StackPanel Margin="40,60,40,40" VerticalAlignment="Center">
                        <!-- Header -->
                        <TextBlock Text="Welcome to SmilezStrap" FontSize="24" FontWeight="SemiBold" 
                                 Foreground="White" Margin="0,0,0,8"/>
                        <TextBlock Text="Launch Roblox or Roblox Studio to get started" FontSize="13" 
                                 Foreground="#999" Margin="0,0,0,30"/>

                        <!-- Action Buttons -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Launch Roblox -->
                            <Button Grid.Column="0" Height="100" Style="{StaticResource PrimaryButton}" 
                                  Click="LaunchRoblox_Click">
                                <StackPanel>
                                    <TextBlock Text="▶" FontSize="28" Foreground="White" 
                                             HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="Launch Roblox" FontSize="14" FontWeight="SemiBold" 
                                             Foreground="White" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <!-- Launch Studio -->
                            <Button Grid.Column="2" Height="100" Style="{StaticResource SecondaryButton}" 
                                  Click="LaunchStudio_Click">
                                <StackPanel>
                                    <TextBlock Text="🔧" FontSize="28" 
                                             HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                    <TextBlock Text="Launch Studio" FontSize="14" 
                                             Foreground="White" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <!-- Info Section -->
                        <Border Background="#252525" CornerRadius="6" Padding="15" Margin="0,25,0,0">
                            <StackPanel>
                                <TextBlock Text="ℹ Quick Info" FontSize="12" FontWeight="SemiBold" 
                                         Foreground="#0078D4" Margin="0,0,0,8"/>
                                <TextBlock Text="SmilezStrap automatically manages your Roblox installation and keeps it up to date." 
                                         FontSize="11" Foreground="#AAA" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Window>
