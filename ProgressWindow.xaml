<Window x:Class="SmilezStrap.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmilezStrap" Height="180" Width="450"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Topmost="True">

    <Border Background="Transparent" Padding="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>

        <Border Background="#1E1E1E" CornerRadius="8" BorderThickness="1" BorderBrush="#2A2A2A">
            <Grid Margin="25,20,25,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0" Text="SmilezStrap" Foreground="White" FontSize="16" 
                         FontWeight="SemiBold" Margin="0,0,0,15"/>

                <!-- Status Text -->
                <TextBlock Grid.Row="1" x:Name="StatusText" Text="Initializing..." Foreground="White" 
                         FontSize="13" Margin="0,0,0,10"/>

                <!-- Progress Bar -->
                <Grid Grid.Row="2" Margin="0,0,0,10">
                    <Border Background="#252525" Height="6" CornerRadius="3">
                        <Border Background="#0078D4" x:Name="ProgressBarFill" Height="6" CornerRadius="3" 
                              HorizontalAlignment="Left" Width="0"/>
                    </Border>
                </Grid>

                <!-- Percentage and Detail -->
                <Grid Grid.Row="3">
                    <TextBlock x:Name="PercentText" Text="0%" Foreground="#999" FontSize="11" 
                             HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <TextBlock x:Name="DetailText" Text="" Foreground="#666" FontSize="10" 
                             HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0"/>
                </Grid>

                <!-- Buttons -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="CancelButton" Content="Cancel" Width="75" Height="28" 
                          Background="#2A2A2A" Foreground="White" BorderThickness="0"
                          FontSize="12" Cursor="Hand" Click="CancelButton_Click" Visibility="Visible">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#353535"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button x:Name="CloseButton" Content="Close" Width="75" Height="28" 
                          Background="#0078D4" Foreground="White" BorderThickness="0"
                          FontSize="12" FontWeight="Medium" Cursor="Hand" 
                          Click="CloseButton_Click" Visibility="Collapsed">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1084D8"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
